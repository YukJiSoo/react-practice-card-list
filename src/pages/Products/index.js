import * as React from 'react';

import { Header } from 'containers/Products';

const Products = () => {
    console.log(Header);
    return (
        <>
            <Header />
        </>
    );
};

export default Products;
